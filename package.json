{
  "name": "npm-safe",
  "version": "0.1.0",
  "description": "Secure npm install wrapper CLI",
  "type": "module",
  "bin": {
    "npm-safe": "./bin/npm-safe.js",
    "npx-safe": "./bin/npx-safe.js"
  },
  "scripts": {
    "test": "node --test src/**/*.test.js",
    "lint": "eslint src/"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "files": [
    "bin/",
    "src/",
    "sandbox/Dockerfile"
  ],
  "license": "MIT",
  "dependencies": {
    "axios": "^1.14.0",
    "chalk": "^5.6.2",
    "commander": "^14.0.3",
    "dockerode": "^4.0.10"
  }
}
